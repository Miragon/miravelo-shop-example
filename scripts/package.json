{
  "name": "miravelo-dev-scripts",
  "version": "0.0.0",
  "private": true,
  "description": "Local dev tooling for the parallel-dev workflow (portless reverse proxy).",
  "scripts": {
    "dev": "./dev.sh up",
    "dev:down": "./dev.sh down"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "portless": "0.14.0"
  }
}
